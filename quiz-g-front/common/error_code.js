let kvArray = [
    ['auth-1', 'Email or password is wrong'],
    ['auth-2', 'name is wrong'],
    ['auth-3', 'Email is wrong format'],
    ['auth-4', 'Email has been used'],
    ['auth-5', 'Password must have more than 5 character'],
    ['auth-6', 'Confirm password is wrong'],
]

let errorMap = new Map(kvArray)

export default errorMap